import {database} from '../db/database.js'

////////////////////////
///        delete report
///////////////////////
function teleteR(arr ,by){
    const index = fineindex((report)=> report.id === id);
     if (index === -1)
        throw new Error("not found id ")

    
    
}
export { teleteR }

const fruits = [1  ,6 ,3  ,4];
console.log(fruits.sort());
